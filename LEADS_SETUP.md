# 📋 Настройка системы заявок

## ✅ Что уже сделано:

### 1. База данных
- ✅ Создана таблица `leads` для хранения заявок
- ✅ Поля: id, name, phone, email, website, message, created_at, status, notes
- ✅ Индексы для быстрого поиска по дате и статусу

### 2. Backend функция
- ✅ Создана функция `/backend/leads/index.py`
- ✅ POST запрос - сохранение новой заявки
- ✅ GET запрос - получение списка заявок для админки
- ✅ Написаны тесты в `tests.json`

### 3. Frontend
- ✅ Компонент формы `ContactForm.tsx` с валидацией
- ✅ Страница админки `/admin` для просмотра заявок
- ✅ Форма подключена на главной странице сайта

---

## ⚠️ Что нужно сделать:

### Шаг 1: Обновить подписку
Вы достигли лимита backend функций (5 из 5). Нужно:
1. Перейти на https://poehali.dev/p/pay
2. Обновить подписку для увеличения лимита функций
3. Вернуться сюда и выполнить команду деплоя

### Шаг 2: Развернуть backend функцию
После обновления подписки выполните в чате с Юрой:
```
Задеплой backend функцию leads
```

Или напишите просто:
```
sync backend
```

### Шаг 3: Проверить работу
1. Откройте главную страницу сайта
2. Заполните форму "Получить консультацию"
3. Отправьте заявку
4. Перейдите на `/admin` чтобы увидеть заявки

---

## 🎯 Как пользоваться админкой:

### Доступ к админке:
- URL: `https://ваш-домен.ru/admin`
- Или в разработке: `http://localhost:5173/admin`

### Возможности:
- ✅ Просмотр всех заявок
- ✅ Фильтрация по статусу (Новые, В работе, Завершены)
- ✅ Информация о клиенте: имя, телефон, email, сайт, сообщение
- ✅ Быстрые действия: позвонить, написать в Telegram
- ✅ Дата и время создания заявки

---

## 📊 Структура данных заявки:

```json
{
  "id": 1,
  "name": "Иван Иванов",
  "phone": "+7 (999) 123-45-67",
  "email": "ivan@example.com",
  "website": "https://example.com",
  "message": "Хочу заказать SEO продвижение",
  "createdAt": "2024-11-27T16:45:00",
  "status": "new",
  "notes": null
}
```

### Статусы заявки:
- `new` - Новая (по умолчанию)
- `in_progress` - В работе
- `completed` - Завершена
- `cancelled` - Отменена

---

## 🔧 Технические детали:

### Backend функция: `/backend/leads/index.py`
- Язык: Python 3.11
- База данных: PostgreSQL (простые запросы)
- Библиотеки: psycopg2-binary

### API endpoints:
- `POST /api/leads` - создать новую заявку
- `GET /api/leads` - получить список заявок
- `GET /api/leads?status=new` - фильтр по статусу
- `GET /api/leads?limit=50` - лимит заявок

### Требуемые ENV переменные:
- `DATABASE_URL` - DSN подключения к БД (уже настроено в poehali.dev)

---

## 💡 Следующие шаги (опционально):

### 1. Добавить уведомления в Telegram
- Создать Telegram бота
- При новой заявке отправлять уведомление

### 2. Добавить изменение статуса заявки
- Кнопки "Взять в работу", "Завершить"
- Добавление заметок к заявке

### 3. Экспорт заявок
- Скачать заявки в CSV/Excel
- Фильтры по дате создания

### 4. Аналитика
- Статистика по заявкам (сколько в день/неделю)
- Конверсия из посетителей в заявки

---

## 📞 Если что-то не работает:

1. Проверьте, что функция задеплоена: смотрите файл `/backend/func2url.json`
2. Проверьте логи функции в poehali.dev
3. Убедитесь, что таблица `leads` создана в БД
4. Напишите Юре: "Проверь почему не работают заявки"
